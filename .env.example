# 「识别数据」服务配置示例（仅本机开发使用）
#
# 使用方法（已从旧版 VITE_ 变量迁移）：
#   1. 编辑项目根目录 .env.local，把旧行 VITE_OPENCODE_GO_API_KEY=xxx
#      重命名为 OPENCODE_GO_API_KEY=xxx（其它旧 VITE_OPENCODE_GO_* 行同理）；
#   2. 重启 dev 服务（npm run dev）后生效。

# ---------------------------------------------------------------------------
# 「识别数据」结构化服务配置（仅本机开发使用）
# ---------------------------------------------------------------------------
#
# 重要性：
# - 这些变量只在 Vite dev server 的 Node 侧（本地代理）读取，**不会注入浏览器**，
#   也不会进入构建产物（dist/），请放心填写；
# - 浏览器只请求同源 /api/recognize-report，由本地代理在 Node 侧添加鉴权后转发；
# - .env.local 已被 .gitignore 忽略，不要提交、不要分享任何含密钥的文件；
# - 旧变量名 VITE_OPENCODE_GO_* 可被代理兼容读取，但前端源码不再引用它们。

# 主/备上游顺序：主上游 = 直连 DeepSeek（下面的 DEEPSEEK_*）；备上游 = OpenCode Go
# （下面的 OPENCODE_GO_*，仅在 DeepSeek 额度类失败时回退）。

# ---------------------------------------------------------------------------
# 主上游：直连 DeepSeek
# ---------------------------------------------------------------------------
#
# 优先级与 fallback 规则：
#   1) 每次识别**总是先**请求主上游直连 DeepSeek（下面的 DEEPSEEK_*）；
#   2) 仅当 DeepSeek 返回 429 / 402（限流/需付费）或响应体出现明显的额度/配额
#      耗尽关键词（额度不足 / quota exhausted 等）时，才自动切到备上游 OpenCode Go；
#   3) 其它错误（401/403/500/502/504/网络/超时）**不会**触发 fallback，仍按原有
#      清洗逻辑返回通用中文错误提示；
#   4) 备上游失败时同样返回其清洗后的错误提示（不再次 fallback）；
#   5) 未配置下面的 OPENCODE_GO_API_KEY 则**不启用** fallback（主上游额度类错误直接返回）。
#
# 安全边界：DEEPSEEK_* 与 OPENCODE_GO_* 一样，只在 Vite dev 的 Node 侧（本地代理）
# 读取，**不会注入浏览器 / 不会进入构建产物**；识别时仍只发送 OCR 文本，绝不发送
# 图片 / 密钥 / 历史报告。
#
# 提示：deepseek-v4-flash 为推理模型，识别请求已显式关闭 thinking（跳过思维链），
# 无需额外配置。

DEEPSEEK_API_KEY=
DEEPSEEK_ENDPOINT=https://api.deepseek.com/chat/completions
DEEPSEEK_MODEL=deepseek-v4-flash
DEEPSEEK_AUTH_HEADER=Authorization
DEEPSEEK_AUTH_SCHEME=Bearer

# ---------------------------------------------------------------------------
# 备上游（可选）：OpenCode Go（fallback）
# ---------------------------------------------------------------------------

OPENCODE_GO_API_KEY=
OPENCODE_GO_ENDPOINT=https://opencode.ai/zen/go/v1/chat/completions
OPENCODE_GO_MODEL=deepseek-v4-flash

# 鉴权方式：请按服务商控制台确认后再填写，不要盲猜。
# 默认使用 HTTP 标准 Bearer Token 鉴权（未设置时为 Bearer）：
OPENCODE_GO_AUTH_HEADER=Authorization
OPENCODE_GO_AUTH_SCHEME=Bearer
#
# 若接口需要「无 scheme 的原始 Key」（例如 X-API-Key: <key>），请把
# OPENCODE_GO_AUTH_SCHEME 留空（不带 = 之后的任何内容）：
#   OPENCODE_GO_AUTH_SCHEME=
# 留空时请求头值仅为 Key 本身（不添加空格/scheme）。
