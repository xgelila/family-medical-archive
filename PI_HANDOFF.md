# 家庭体检档案 — 交接摘要（PI Handoff）

> 项目：`/Users/bing/Documents/workspace/family-medical-archive`
> 产品形态：本地优先 PWA（非浏览器插件）。本地存储、完整导出、离线可用；不做诊断/治疗建议。
> 本文件供新会话快速接续上下文；不含密钥与真实健康数据。

## 完成了什么（截至 2026-08-19）

- **识别链路已跑通并修复**：
  - 根因：`deepseek-v4-flash` 是推理模型，默认思维链在复杂 schema 下 >90s 撞超时 → 请求体显式 `thinking: {type:"disabled"}`。
  - 主备上游已对调：**主上游 = 直连 DeepSeek（DEEPSEEK__），备上游 = OpenCode Go（OPENCODE_GO__）**，仅在额度类失败（429/402）回退。opencode-go 每周额度已用尽（约 4 天重置）。
  - 提示词改为**通用规则**：`name` 用医学常识识别正确名称、纠正 OCR 明显错误；`sourceText` 逐字保留溯源。**不写死任何具体项目矫正**。
- **报告元数据保留**：`Report` 增加 `details`（KV 元数据），识别出的送检医生/检验者/审核者/采样/接收/打印日期/临床诊断/检验目的/分院/编号/姓名/性别/年龄/病历号 + extraFields/notes/unresolvedText 全部进「报告详情」折叠区，随保存/导出保留。
- **识别自动填表已修复**：新建报告默认走「整张报告」模式，识别结果非空即覆盖默认值，医院/日期/类型/标题/元数据自动进表单，「识别整张报告」为主按钮。
- **曲线分组**：定稿为 `name + resultKind + unit`，**不依赖受控目录**，宁拆不合；`standardLabel` 遗留已从 trend 移除。
- **受控目录**：保留在代码但**暂停**（32 项、未医学审核、覆盖窄；`糖化血红蛋白Al` 别名有误合并隐患待修）。
- **Git 已初始化**，提交历史：`git log` 可见（首版 → 超时/上游 → 提示词 → 元数据/入口 → 填表修复）。未定义 `security-scan` 脚本，提交前手工确认无 `.env.local`/密钥/构建产物。

## 进行中

- **整体 UI/交互重设计**（用户明确要求，尚未完成）：
  - 已做：新建入口 `NewReportEntry`（拍摄/相册大按钮 + 手动录入次级入口）；报告详情折叠区；精简识别文案。
  - **未做**：表单仍「一页铺开」（成员/医院/日期/类型/标题 + 附件 + 识别面板 + 详情 + 项目表 + 快捷添加堆在一起），用户认为仍古早、不简洁。
  - 用户给了两条路线待选：**A 分步向导**（选成员 → 拍摄/选图 → 自动识别 → 核对页保存，推荐）或 **B 单页分区折叠**。尚未拍板。

## 阻塞

- 无硬阻塞。
- 浏览器工具**禁止访问 loopback（127.0.0.1）**，无法截图验证本地 UI；需用户本地打开 `http://127.0.0.1:5173/` 看效果。

## 待决策：UI 重设计路线（A / B）

用户已明确要求「整体重设计、简洁流畅、不能古早/草稿」。表单目前仍一页铺开，需重构。两条路线：

**路线 A：分步向导（推荐）**

- 流程：① 选成员（可记忆上次）→ ② 拍摄 / 相册 / 手输 → ③ 自动识别并填充（识别进度内联）→ ④ 核对页（医院/日期/项目列表 + 详情折叠）→ 保存。
- 优点：每步只做一件事，移动端友好，彻底解决「一页铺开」；识别结果直接进核对页，无二次点击。
- 代价：ReportForm 需拆成多步向导，改动较大（状态流转 + 新组件）。
- 适合：手机为主、要最简洁。

**路线 B：单页分区折叠**

- 流程：保留单页，但默认只展开「成员 + 医院 + 日期」与「项目列表」，附件/识别/详情/快捷添加全部折叠为二级入口，识别进度做成卡片内联。
- 优点：改动较小、风险低。
- 缺点：只是缓解「铺开感」，未根治。
- 适合：桌面为主、想少动代码。

**推荐 A**：符合用户「简洁流畅」的明确诉求，且本地 PWA 移动端场景下分步向导最自然。

## 下一步

1. 确认 UI 重设计路线（见上方 A/B 对比，推荐 A），落地前先出交互稿/说明再改代码。
2. 重设计完成后，铺到其余页面（概览/列表/趋势/成员/数据管理）。
3. 受控目录：等用户决定是否扩项 + 医学审核 + 修 `糖化血红蛋白Al` 别名；当前曲线不依赖它。
4. 本次会话已完成路线 A 向导初版：新建报告改为三步流程（选成员 → 扫描/手动（有图自动进识别）→ 核对并保存），并修复整张报告识别元数据传递、切换来源时清理旧识别结果、隐藏无效的仅识别入口。
5. **路线 A 向导已按用户 6 点反馈迭代**：无成员提示+去成员页入口；扫描报告主按钮→bottom-sheet（拍摄/相册）；选图自动进图片编辑（tui-image-editor 原生 cropzone 裁剪/pixelate 马赛克，逐张，不承诺擦除原图）；AI schema/prompt 增加 testPurpose=检验目的→映射报告类型；识别成功自动进入 ReportReview（识别整张报告为唯一主 CTA，无二次确认/下一步）；第 3 步改为 ReportReview 核对页（不再挂载旧识别面板，识别结果保留，返回识别页可查看既有候选）。新增 `ScanSourceSheet`/`ImageCropModal` 重构/`ReportReview`/`wizardFlow` 纯逻辑 + 对应测试。依赖新增 `tui-image-editor@3.15.3`。

## 验证命令

- `npm run typecheck`、`npm run build`、`npm run format:check`、`npx vitest run`（当前 218 用例全绿）
- 本地 dev：`npm run dev -- --host 127.0.0.1 --port 5173`；识别接口仅 dev 可用，生产静态部署无该接口。
- 本次验证：`npm run typecheck`、`npm run build`、`npx vitest run`（现 242 用例全绿）全部通过；`npm run format:check` 仅因既有 `PI_HANDOFF.md` 格式警告失败，相关源码定向 prettier 检查通过。
