

## 本次子任务交接（2026-08-19）

### 完成了什么
- 单 exam 影像编辑与管理摘要不再显示“子检查 1”，多 exam 仍显示分组标题。
- `CustomReportType` 增加可选 `reportKind`；新增类型 API、严格 testPurpose 匹配、识别页/核对页保存候选均携带大类，类型下拉按 lab/imaging 隔离；识别候选只有用户点击后持久化。
- 导入清洗保留合法 `reportKind`。

### 进行中
- imaging 长检查项目的“分别保存候选/联合检查”尚未实现；当前识别页仅对单一未匹配候选提供保存。

### 阻塞
- 无。

### 下一步
- 补充/审查 imaging 长文本拆分候选交互及对应组件测试，避免将整段文本直接作为单个类型。
